{% extends 'base.html.twig' %}

{% block title %}Édition du document{% endblock %}

{% block main %}
	<section class="admin">
		<div class="col1">
			<nav class="admin-nav">
				{% include "partial/_menuBackstage.html.twig" %}
			</nav>
			
		</div>
		<div class="col2">
			<div class="title">
				<h3>
					Édition du document
				</h3>
			</div>
			<div class="admin-panel">
                <section>
                    {{ include('file/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                </section>
                <section class="boutons">
                    <a class="bt-form" href="{{ path('file_index') }}">Retour à la liste</a>
                    {{ include('file/_delete_form.html.twig') }}
                </section>

				


			</div>
		</div>


	</section>
    
{% endblock %}
